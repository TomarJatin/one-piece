/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', "comicvine.gamespot.com", "i.ibb.co", "d28hgpri8am2if.cloudfront.net", "storage.googleapis.com", "cdn.readonepiece.com"],
      },
}

module.exports = nextConfig
