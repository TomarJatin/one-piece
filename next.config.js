/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', "manga18fx.com", "cdn.readdetectiveconan.com", "i.ibb.co", "d28hgpri8am2if.cloudfront.net", "storage.googleapis.com"],
      },
}

module.exports = nextConfig
